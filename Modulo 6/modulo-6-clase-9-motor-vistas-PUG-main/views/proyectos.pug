extends layout
block content
  .container.py-5
    h1.mb-4 #{title}
    .row
      each proyecto in proyectos
        .col-md-4.mb-4
          .card.shadow.h-100
            .card-body
              h5.card-title #{proyecto.nombre}
              p.card-text #{proyecto.descripcion}
